{
  "version": 2,
  "installCommand": "cd ../.. && pnpm install && pnpm --filter @shopvendly/db build && pnpm --filter @shopvendly/auth build",
  "builds": [
    {
      "src": "index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.ts"
    }
  ]
}